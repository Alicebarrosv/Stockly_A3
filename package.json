{
  "name": "estoque-frontend",
  "version": "0.1.0",
  
  
  "homepage": "https://srloki11.github.io/Stockly", 
  
  "private": true,
  "dependencies": {
    "react": "^18.2.0"
    
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    
    
    "predeploy": "npm run build", 
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
    
  }
}